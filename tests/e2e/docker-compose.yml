version: "3.7"

services:
  test-salary-api:
    build: ./api
    depends_on:
    - test-db

  test-db:
    image: postgres:alpine,
    environment:
      - POSTGRES_PASSWORD=password

  testmace:
    build: ./TestMace
    volumes:
      - ./out:/out
    depends_on:
    - test-salary-api
